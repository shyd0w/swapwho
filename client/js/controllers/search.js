angular
  .module('app')
  .controller('SearchController', ['$scope', '$state', 'Item', 'Category', 'Swapper', 'Statistics', function($scope,
      $state, Item, Category, Swapper, Statistics,  ) {
      
  }]);