angular
  .module('app')
  .controller('SearchController', ['$scope', '$state', 'Item', 'Category', 'Statistics', 'Swapper', function($scope,
      $state, Item, Category, Statistics, Swapper) {
      
  }]);